[package]
name = "port_scanner"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
clap = {version = "4.6.0", features = ["derive"]}
tokio = {version = "1.52.3", features = ["full"]}
